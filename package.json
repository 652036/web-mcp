{
  "name": "forkcast-webmcp",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A human-agent decision studio built for WebMCP.",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/verify.mjs",
    "build": "node scripts/build.mjs",
    "smoke": "node scripts/smoke.mjs",
    "check:prod": "node scripts/check-prod.mjs https://forkcast.st2p8g4tkf.chatgpt.site/",
    "verify": "npm run check && npm test && npm run build && npm run smoke"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT"
}
